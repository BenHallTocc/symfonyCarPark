{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Stay list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Entrytime</th>
                <th>Exitbytime</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('stay_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.entryTime %}{{ entity.entryTime|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.exitByTime %}{{ entity.exitByTime|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('stay_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('stay_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('stay_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
