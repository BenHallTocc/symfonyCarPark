{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Stay</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Entrytime</th>
                <td>{{ entity.entryTime|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Exitbytime</th>
                <td>{{ entity.exitByTime|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('stay') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('stay_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
