{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Driver creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('driver') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
